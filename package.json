{
  "name": "hive-bedrock",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^8.2.4"
  }
}
